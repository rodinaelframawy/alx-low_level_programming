#include "main.h"
/**
 * print_alphabet_x10 - prints the alphabet in lowercase 10 times
 * Return: 0 (success)
 */
void print_alphabet_x10(void)
{	int i;
	int j;

	for (i = 0; i < 10; i++);
{
	for (j = 97; j <= 132; j++);
	{ 
		_putchar(j);
	}
	_putchar('\n');
}

return 0;
}
